# Downloads Panel Test Plan

**File Under Test:** `lib/features/downloader/presentation/widgets/downloads_panel.dart`
**Target Layer:** Presentation / Widgets
**Coverage Target:** >90%

## Widget Test Plan

| Test ID | File(s) Under Test | Target Widget / State | Scenario (It should...) | Setup & Mocks (Given) | Action (When) | Assertions (Then) |
|---|---|---|---|---|---|---|
| W-DL-PAN-01 | `downloads_panel.dart` | `DownloadsPanel` | switch IndexedStack child for tasks/history/history-detail views | override `downloadsPanelViewProvider` with each enum value | pump widget | renders `_MediaDownloaderPanel`, `DownloadHistoryView`, or `DownloadHistoryDetailView` at the correct index |
| W-DL-PAN-02 | `downloads_panel.dart` | `_MediaDownloaderPanel.initState` | request URL focus on first frame when tasks view is active | tasks view active and focusable input | pump and settle | URL field receives focus |
| W-DL-PAN-03 | `downloads_panel.dart` | `_checkBinaries` | render missing-binaries view until required engines are available | mock `EngineRegistry.requiredInstalled=false` and then true | pump widget / trigger recheck | missing-binaries view appears first, then normal panel after recheck |
| W-DL-PAN-04 | `downloads_panel.dart` | `ref.listen(downloadUrlFocusRequestProvider)` | re-focus the URL box when focus request provider ticks | panel already mounted | mutate focus-request provider | URL field becomes focused again |
| W-DL-PAN-05 | `downloads_panel.dart` | `ref.listen(downloadsPanelViewProvider)` | re-focus the URL input when returning to tasks view | move away from tasks then back | update provider | input focus is restored on tasks view re-entry |
| W-DL-PAN-06 | `downloads_panel.dart` | `_handleKeyEvent` | toggle active-download drawer with `Ctrl+\`` only when panel is open | `downloadsPanelOpenProvider=true` and active tasks present | send control+backquote | drawer open flag flips |
| W-DL-PAN-07 | `downloads_panel.dart` | `CallbackShortcuts` escape | clear selection and close preview overlays with `Escape` | list selected or preview visible | send `Escape` | selected indices clear and preview closes |
| W-DL-PAN-08 | `downloads_panel.dart` | `CallbackShortcuts` escape priority | dismiss error logs overlay before other escape behavior | `_errorLogsMessage` visible plus selection present | press `Escape` | logs overlay closes while other state remains intact |
| W-DL-PAN-09 | `downloads_panel.dart` | `CallbackShortcuts` escape unsaved dialog | dismiss unsaved confirmation and pending action with `Escape` | unsaved confirmation visible | send `Escape` | confirmation closes and pending clear action is cleared |
| W-DL-PAN-10 | `downloads_panel.dart` | `CallbackShortcuts` delete | remove selected parsed groups from the list | parsed items present and selected | send `Delete` | selected groups disappear and list stats refresh |
| W-DL-PAN-11 | `downloads_panel.dart` | `CallbackShortcuts` ctrl+a | select all currently filtered items | multiple filtered entries visible | send `Ctrl+A` | every filtered row becomes selected and anchor/last-selection are updated |
| W-DL-PAN-12 | `downloads_panel.dart` | `CallbackShortcuts` arrow navigation | move single selection up/down through filtered items and auto-scroll | loaded list with multiple rows | send arrow keys | selection follows visual order and selected item is kept visible |
| W-DL-PAN-13 | `downloads_panel.dart` | `CallbackShortcuts` shift+arrow navigation | expand range selection using current anchor | filtered list loaded | send `Shift+ArrowUp/Down` | contiguous range becomes selected |
| W-DL-PAN-14 | `downloads_panel.dart` | `CallbackShortcuts` ctrl+s | update an imported list only when path exists and list changed | imported file path set, dirty and clean variants | send `Ctrl+S` | dirty imported list calls `_updateList`; clean list no-ops |
| W-DL-PAN-15 | `downloads_panel.dart` | `_showLocalToast` | show a transient toast and hide it after delay | call path that succeeds or fails | trigger toast-producing action | overlay animates in with message and disappears after 3 seconds |
| W-DL-PAN-16 | `downloads_panel.dart` | `_exportList` | export parsed items to JSON using file picker target and then clear panel state | file picker returns save path, parsed items/statistics present | tap Export | file is written, list/configs/selection/preview/import metadata reset, success toast shown |
| W-DL-PAN-17 | `downloads_panel.dart` | `_exportList` error path | show failure toast when write/export fails | file write throws | tap Export | list remains intact and error toast appears |
| W-DL-PAN-18 | `downloads_panel.dart` | `_updateList` | overwrite imported JSON file and clear dirty flag | imported path present with parsed items | trigger Update or `Ctrl+S` | file contents refresh and `_isListChanged=false` |
| W-DL-PAN-19 | `downloads_panel.dart` | `_importList` | import valid JSON list/map files into parsed items and per-group configs | file picker/path returns valid JSON | tap Import or drop file | groups load, configs initialize, import metadata populates, selection resets, success toast shows |
| W-DL-PAN-20 | `downloads_panel.dart` | `_importList` invalid JSON | keep panel stable and show `Invalid JSON file` toast | chosen file contains malformed JSON | import file | toast appears and prior state is preserved |
| W-DL-PAN-21 | `downloads_panel.dart` | drag-and-drop surface | accept JSON drops, reject non-JSON drops, and block import when list already has content | empty and non-empty panel variants | drag file over panel and drop | overlay messaging changes, supported JSON imports, unsupported/conflicting drops show toasts |
| W-DL-PAN-22 | `downloads_panel.dart` | `_analyzeUrls` placeholder flow | clear input, inject loading placeholders, assign config engine, and mark list dirty | URL input contains unique and duplicate URLs | submit analysis | placeholders appear once per URL and stats refresh immediately |
| W-DL-PAN-23 | `downloads_panel.dart` | `_analyzeUrls` result handling | replace placeholders with success groups, error groups, and default filters/formats | backend returns image-only, video-only, playlist/profile, empty, and thrown variants | await analysis futures | groups update correctly, default filters match media mix, playlists/profiles trigger hydration when small, `_error` is set on failure |
| W-DL-PAN-24 | `downloads_panel.dart` | `_hydrateProfile` progressive path | show live hydration state, append progress items, throttle stats, and auto-select best final format | playlist/profile group with backend progress stream | await progressive hydration | loading trackers update, placeholder ordering is preserved, final sorted items replace provisional ones, `downloadTaskProvider.onHydrationFinished` fires |
| W-DL-PAN-25 | `downloads_panel.dart` | `_visiblePreviewItems` | filter preview media by group filter and keep hydration placeholder only when needed | preview group includes playlist/profile head plus mixed media | open preview under different filters/loading states | returned visible list matches filter and hydration rules |
| W-DL-PAN-26 | `downloads_panel.dart` | `_removeParsedItems` | abort active hydration pids, rebuild contiguous configs, clear selection, clear preview, and unfocus when list empties | parsed items with active hydration and sparse config indices | remove one or all groups | active pids are killed when requested, configs are reindexed, state flags reset correctly |
| W-DL-PAN-27 | `downloads_panel.dart` | `_removeSingleItem` | remove one child item or the entire group when last item disappears | previewed group with one or multiple child items | trigger remove-single path | target child is removed and parent group is dropped only when empty |
| W-DL-PAN-28 | `downloads_panel.dart` | `_startDownload` grouped flow | enqueue grouped/profile/playlist downloads with correct destination, filterType, totalItems, expectedBytes, and browser settings | mixed group, settings for current-folder toggle, hydration states | trigger group download | notifier receives the right `startDownload` payload and source group is removed afterward |
| W-DL-PAN-29 | `downloads_panel.dart` | `_startDownload` single-item flow | sanitize titles, trim long names, resolve filename collisions, choose item-level formats, and remove only downloaded children | single item or single-item-from-group download with existing files | start download | final title/path/format arguments match rules and only targeted items are removed |
| W-DL-PAN-30 | `downloads_panel.dart` | `_startDownloadAll` | batch selected or full filtered groups, prevent session-name collisions, and reset sort filter afterward | multiple groups selected with conflicting names | tap `Download All` / `Download N` | notifier receives all expected downloads, processed groups are removed, and `_sortFilter` resets to `added_desc` |
| W-DL-PAN-31 | `downloads_panel.dart` | active-download strip | show aggregate progress bar, toggle drawer, render tiles, and expose `Cancel All` confirmation | active tasks provider returns running/pending tasks | tap strip and cancel button | summary bar reflects aggregate progress, drawer opens, tiles render, cancel confirmation can abort all tasks or dismiss |
| W-DL-PAN-32 | `downloads_panel.dart` | `_buildErrorLogsOverlay` | display pipeline logs, copy them, refresh live text, and close via icon or backdrop | `_errorLogsMessage` and `_activeLogItem` seeded | open overlay and interact | logs render in read-only field, copy icon toggles to checkmark temporarily, refresh recomputes text, close paths dismiss overlay |
| W-DL-PAN-33 | `downloads_panel.dart` | `_buildUnsavedConfirmationOverlay` | block destructive clear actions when imported list has unsaved edits | imported list is dirty and user requests clear/close | trigger clear action | dialog shows, `Cancel` preserves state, `Close Anyway` runs stored action |
| W-DL-PAN-34 | `downloads_panel.dart` | `_JugglingBallsLoader` / `_GradientBorderPainter` | render the custom loading and animated border primitives without regressions | pump widgets directly | animate one frame | three bouncing dots render and custom painter repaints when animation/colors/radius/stroke change |

