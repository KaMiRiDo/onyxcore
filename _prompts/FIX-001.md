I am planning to change tag functionality a bit. It is mostly a ui enhancement for tag editor only. As of now the notch pointer accuracy and the context menu and all are working fine. Also the marker implementation and logic is good, we dont need to touch that. So please don't touch already existing functionalities, just enhance ui as requested below ( wireframe is attached):

1. The emojis section is changing a bit to add icons as well. From here onwards , the emojis/icons are only selectable. It means user can select an icon/emoji so that it will be come in the marker. The marker text will be just a text only field.User cannot add emojis to it

2. When user hits the T short key or user edit an already existing marker, i need a UI ( as attached image 1) shound be oppened out. SO there is only two fields , one for text input, an other to set the icon for the marker. The recent tag icons box only need to contain a fixed number of recently used icons/emojis. Add the ddefault icon pin to the list and keep it selected by default. The selectoon box should keep the same style of the actual marker with the notch , so user can eaasily identify the use case. The reset button there should be a clear icon , clicking on  which i need to clear other icons/emojis in the recent section, keep the default one only there.

3. When user clicks the top expand icon in the initial UI , i need the expanded view to be open ( as in image 2 ). There we will have all the other emojis and icons. As of now keep only two categoris, Emojis and Icons ( can be represented by icons insead of text). All the emojis can be listed under that, we dont need separet lists. In the emojis section down bottom corner of the emojis container , i need a add icon button + , clicking on which i need to open the emojis editor UI as we have alredy implemented.

4. The icons tab can be empty as of now , which an add icon on the bottom right corner of that container. We will implemet the add UI later.

5. The search section in the UI should work as of now. 

6. You can keep the UI styling as implemented now. Like the style of search box and all keep it as implemented now. Keep the attached wireframe as the base or just a reference and enhance it.

Give me an implementation plan and do the implementation only after my approval.

